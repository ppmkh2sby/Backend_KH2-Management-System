# Keep this relative path in production so Nginx forwards requests to the backend.
VITE_API_BASE_URL=/api/v1

# Only used by `npm run dev`; this is the backend HTTP launch profile.
VITE_DEV_BACKEND_URL=http://localhost:5132
